using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerProgress : MonoBehaviour
{
    //THIS SCRIPT HOLDS PLAYERS VALES FOR LEVELS AND ORBS

    public int TotalHealthOrbs, TotalWeaponOrbs, TotalArmorOrbs, TotalMovementOrbs, TotalEquipmentOrbs;

    // Start is called before the first frame update
    void Start()
    {
        
    }
}
